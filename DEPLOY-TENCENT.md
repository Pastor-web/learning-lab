# 用腾讯云把学习站放到公网（COS 静态网站）

适合：`C:\Users\Animamm\Desktop\FDE学习` 这类纯静态站（HTML/CSS/JS）。  
整站约 **1MB 以内**，用对象存储 COS 即可，**不必买云服务器**。

官方参考（政策以控制台为准）：

- 对象存储 COS：https://console.cloud.tencent.com/cos  
- 新用户免费额度说明：https://cloud.tencent.com/document/product/436/6240  

---

## 你会得到什么

部署成功后，网友用浏览器打开类似地址即可（示例）：

```text
https://你的桶名.cos-website.ap-guangzhou.myqcloud.com
```

或绑定自己的域名后：

```text
https://fde.你的域名.com
```

---

## 开始前准备

| 准备项 | 说明 |
| --- | --- |
| 腾讯云账号 | https://cloud.tencent.com 注册 |
| **实名认证** | 个人身份证即可；未实名很多能力不可用 |
| 支付方式 | 常需绑定微信/银行卡；免费额度内一般不扣费，超出才扣 |
| 本地文件夹 | `C:\Users\Animamm\Desktop\FDE学习` 全部文件 |

**费用提醒：**

- 新用户常见礼包：**约 50GB 标准存储 / 约 6 个月**（以账号「资源包」为准）  
- 流量、请求超出免费包后按量计费  
- 建议在费用中心设置 **预算告警**（如每月 5～10 元）  

---

## 第一步：开通 COS

1. 登录 https://console.cloud.tencent.com/cos  
2. 若提示开通对象存储，点 **开通**  
3. 打开 **资源包管理** / 费用相关页，确认是否有 **免费额度包**  

---

## 第二步：创建存储桶

1. COS 控制台 → **存储桶列表** → **创建存储桶**  
2. 建议设置：  

| 项 | 建议值 |
| --- | --- |
| 名称 | 全球唯一，如 `fde-lab-你的昵称`（只能小写字母、数字、连字符） |
| 所属地域 | 离你近：如 **广州 ap-guangzhou** / **上海** |
| 访问权限 | 先选 **私有读写**，后面用「静态网站 + 策略」或改为公有读（见下文） |
| 其他 | 默认即可 |

3. 创建完成，记住：**桶名称** 和 **地域**。

---

## 第三步：打开「静态网站」

1. 进入该存储桶 → 左侧 **基础配置**（或 **安全管理 / 静态网站**，以界面为准）  
2. 找到 **静态网站** → **编辑** → **开启**  
3. 配置：  

| 项 | 值 |
| --- | --- |
| 索引文档 | `index.html` |
| 错误文档 | 可填 `index.html`（简单站点够用） |

4. 保存后，页面会显示 **静态网站访问节点**，形如：  

```text
https://<BucketName>.cos-website.<Region>.myqcloud.com
```

先复制保存，上传文件后再访问。

---

## 第四步：权限（让网友能打开）

静态网站需要匿名用户能读对象。常用两种做法（二选一）：

### 做法 A：存储桶设为「公有读私有写」（最简单）

1. 存储桶 → **权限管理** → **存储桶访问权限**  
2. 改为 **公有读私有写**  
3. 确认风险提示（谁知道链接都能读你桶里的文件——本站本就是公开学习内容，一般可接受）

### 做法 B：用策略只放开读（更精细）

在 **权限管理 → 策略权限** 增加允许 `GetObject` 给所有人（`*`）的策略。  
（控制台有「公共读」模板时可直接用。）

> 不要把 **密钥 SecretId/SecretKey** 写进网页代码。本站纯前端，不需要密钥。

---

## 第五步：上传网站文件

### 方式 1：控制台网页上传（文件少时可用）

1. 存储桶 → **文件列表** → **上传文件**  
2. 上传 **整个站点根目录下的文件和文件夹**，保持相对路径：  

```text
index.html
styles.css
app.js
data.js
book-content.js
essays-content.js
NOTICE.md
README.md
DEPLOY-TENCENT.md
vendor/marked.min.js
book/...（可选，正文已在 book-content.js）
essays/...（可选）
scripts/...（可选，可不上传）
```

**注意：**

- `index.html` 必须在桶的 **根目录**，不能放在子文件夹里  
- 文件夹 `vendor` 要一起上传，否则 Markdown 渲染会挂  
- `book-content.js`、`essays-content.js` **必须上传**  

### 方式 2：COSBrowser 客户端（推荐）

1. 下载 **COSBrowser**：https://cloud.tencent.com/document/product/436/11366  
2. 用 API 密钥登录（控制台 → 访问管理 CAM → API 密钥）  
3. 选中本地 `FDE学习` 文件夹，拖到桶根目录  

### 方式 3：命令行（熟悉的话）

安装 [COSCMD](https://cloud.tencent.com/document/product/436/10976) 后：

```bash
coscmd config -a <SecretId> -s <SecretKey> -b <BucketName-APPID> -r ap-guangzhou
coscmd upload -r "C:\Users\Animamm\Desktop\FDE学习/" /
```

---

## 第六步：用手机 / 电脑公网打开

1. 浏览器打开第三步的 **静态网站访问节点**  
2. 应看到「FDE 学习实验室」首页  
3. 检查：  
   - [ ] 首页致谢（XDash + 三篇文章作者）  
   - [ ] 学习地图能进章节原文  
   - [ ] 延伸阅读能打开  
   - [ ] 白天/黑夜切换  

若打不开：

- 是否开了静态网站  
- `index.html` 是否在根目录  
- 权限是否公有读  
- 链接是否用的是 **cos-website** 节点（不是普通 cos 下载域名）  

---

## 第七步：费用告警（强烈建议）

1. 控制台 → **费用中心** → **预算与告警**（名称以现网为准）  
2. 设置月预算，如 **10 元**，超 50%/80% 短信提醒  
3. 关注：存储容量、**外网下行流量**  

公开分享后若被刷流量，费用会涨——告警能兜底。

---

## 可选：绑定自己的域名（更好记）

1. 你有已备案域名（国内访问建议备案）  
2. 存储桶 → **域名与传输管理** → **自定义源站域名** / 静态网站域名（以界面为准）  
3. 按提示加 CNAME 解析到 COS 给出的域名  
4. 配置 HTTPS 证书（腾讯云可申请免费证书）  

无域名可跳过，直接用 `cos-website` 链接分享。

---

## 可选：叠 CDN（流量大再开）

访问量大时再开 CDN 加速 COS，能减轻源站流量费，但配置更复杂。  
小流量学习站 **先不用 CDN**。

---

## 以后如何更新网站

本地改完 `FDE学习` 后：

1. 重新上传改动过的文件（覆盖）  
2. 若改了 `book/*.md`：先运行  

```powershell
cd "C:\Users\Animamm\Desktop\FDE学习"
python scripts\build_book.py
python scripts\gen_essays.py
```

3. 再上传新的 `book-content.js` / `essays-content.js` 等  
4. 浏览器 **强制刷新**（Ctrl+F5）  

---

## 分享给网友时建议附带

> 非官方、非盈利 FDE 学习站。  
> 主线感谢范冰（XDash）开源书；延伸阅读感谢 @AdrianPunk115、@Formulasearch。  
> 链接：`你的静态网站地址`

---

## 和「小程序」的关系

- **腾讯云 COS** = 公网 **网页**，微信里可点链接打开（部分环境可能限制外链）  
- **微信小程序** = 另一套注册与开发，个人不能直接把整个网页嵌进小程序  
- 当前文档只解决：**腾讯云公开网页**  

---

## 常见问题

**Q：一定要买服务器吗？**  
A：不用。COS 静态网站即可。

**Q：完全免费吗？**  
A：新用户常有数月免费包；过期或超流量会按量计费。务必看资源包 + 设告警。

**Q：必须备案吗？**  
A：用默认 `*.myqcloud.com` 静态网站域名一般可先访问；**绑定自己的域名在国内做网站通常要备案**。

**Q：上传后样式乱了？**  
A：检查 `styles.css`、`app.js`、`vendor/marked.min.js` 路径是否与本地一致，是否都在根目录相对位置。

---

## 你现在的操作清单（打勾）

1. [ ] 腾讯云注册 + 实名  
2. [ ] 开通 COS，确认免费包  
3. [ ] 创建存储桶  
4. [ ] 开启静态网站，`index.html`  
5. [ ] 公有读（或等效策略）  
6. [ ] 上传 `FDE学习` 全部必要文件  
7. [ ] 浏览器打开静态网站节点  
8. [ ] 设置费用告警  
9. [ ] 把链接发给网友  
